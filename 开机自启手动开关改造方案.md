# 开机自启手动开关改造方案

> **目标项目**：`interknot_auth_flutter`（v2.0）
> **改造范围**：在设置页面新增「开机自启」手动开关，支持用户自主开启/关闭，并持久化用户选择
> **难度**：中等（6 处文件改动，均为增量式）
> **预计工期**：0.5 人日

---

## 1. 背景与问题定位

### 1.1 现状

项目已具备完整的开机自启底层能力：

- **`launch_at_startup: ^0.3.1`** 依赖已引入（`pubspec.yaml:39`）。
- **`SystemIntegrationService`** 已封装 `setAutoStart(bool)` 与 `isAutoStartEnabled()` 两方法（`lib/core/platform/system_integration_service.dart:137-171`）。
- **`PlatformService.supportsAutoStart`** 平台能力判断已定义（`lib/core/platform/platform_service.dart:23`）。

### 1.2 问题根因

`lib/app.dart` 第 248-255 行的 `_initAutoStart()` **硬编码 `setAutoStart(true)`**：

```dart
// lib/app.dart:248-255（当前实现）
void _initAutoStart() {
  final platform = ref.read(platformServiceProvider);
  if (!platform.supportsAutoStart) return;

  // 默认启用自启（与 schtasks ONLOGON 行为一致）
  _systemIntegration!.setAutoStart(true);   // ← 问题：每次启动强制开启
  _logger.info('开机自启已配置');
}
```

**三大缺陷**：
1. 每次启动都强制 `setAutoStart(true)`，用户无法关闭。
2. 用户选择不持久化（`ConfigStore` 无对应配置项）。
3. 设置页面无 UI 开关（`settings_page.dart` 只有登录参数/隧道/配置管理三段，无「系统」区块）。

### 1.3 改造目标

- 设置页新增「开机自启」开关，状态与系统真实状态同步。
- 用户切换开关即时生效（调用 `setAutoStart`）并持久化到 `ConfigStore`。
- 启动时读取配置决定是否注册自启，不再硬编码。
- 移动端不支持时禁用开关并给出提示。

---

## 2. 改造方案总览

### 2.1 改动清单（6 处）

| # | 文件 | 改动 | 类型 |
|---|------|------|------|
| 1 | `lib/core/storage/config_store.dart` | 新增 `launchAtStartup` 配置项 getter/setter | 增量 |
| 2 | `lib/core/state/providers.dart` | 新增 `systemIntegrationServiceProvider` | 增量 |
| 3 | `lib/app.dart` | `_initAutoStart()` 改为读配置，去掉硬编码 | 修改 |
| 4 | `lib/features/settings/presentation/settings_page.dart` | 新增「系统」区块 + 开机自启 SwitchListTile | 增量 |
| 5 | `lib/l10n/app_zh.arb` + `app_en.arb` + `app_localizations*.dart` | 新增文案 | 增量 |
| 6 | 平台适配与边界处理 | 移动端禁用、错误回滚、状态同步 | 逻辑 |

### 2.2 数据流

```
启动时：  ConfigStore.launchAtStartup  ──读──►  app._initAutoStart()  ──►  SystemIntegrationService.setAutoStart(v)
                                                                   └─► launch_at_startup 注册/注销

设置页：  SwitchListTile.onChanged(v)
            ├─► SystemIntegrationService.setAutoStart(v)   （即时生效）
            └─► ConfigStore.launchAtStartup = v             （持久化）
          进入页面时： isAutoStartEnabled() ──► 同步开关真实状态（系统可能被外部改）
```

---

## 3. 逐步实现

### 步骤 1：ConfigStore 新增配置项

**文件**：`lib/core/storage/config_store.dart`

**定位**：第 45-53 行「开关配置」区块，`autoConnect` 之后插入。

**改动前**（第 51-53 行）：
```dart
  /// 自动连接
  bool get autoConnect => _sp.getBool('auto_connect') ?? false;
  set autoConnect(bool v) => _sp.setBool('auto_connect', v);
```

**改动后**（在其后追加）：
```dart
  /// 自动连接
  bool get autoConnect => _sp.getBool('auto_connect') ?? false;
  set autoConnect(bool v) => _sp.setBool('auto_connect', v);

  /// 开机自启（用户手动开关）
  ///
  /// 与 `autoConnect`(自动登录) 不同：此开关控制是否注册系统开机自启。
  /// 默认 false，需用户在设置页主动开启。
  bool get launchAtStartup => _sp.getBool('launch_at_startup') ?? false;
  set launchAtStartup(bool v) => _sp.setBool('launch_at_startup', v);
```

> **说明**：默认 `false`。若需兼容旧版本「默认自启」行为，可在 `migration.dart` 迁移时检测旧 schtasks 任务是否存在，存在则置 `true`（见步骤 3 备注）。

---

### 步骤 2：注册 SystemIntegrationService Provider

**文件**：`lib/core/state/providers.dart`

**原因**：当前 `SystemIntegrationService` 在 `app.dart:65` 手动 `new`，设置页无法通过 `ref` 获取。需注册为 Provider，供设置页调用 `setAutoStart`。

**改动**：在文件顶部 import 区追加，并在「平台」区块后新增 Provider。

**import 追加**：
```dart
import '../platform/system_integration_service.dart';
```

**新增 Provider**（置于 `platformServiceProvider` 之后，约第 67 行后）：
```dart
// ──────────────────────────── 系统集成 ────────────────────────────

/// 系统集成服务 Provider
///
/// 依赖 fileStore 与 platformService，供 app.dart 与 settings_page 共用。
final systemIntegrationServiceProvider = Provider<SystemIntegrationService>(
  (ref) => SystemIntegrationService(
    fileStore: ref.watch(fileStoreProvider),
    platformService: ref.watch(platformServiceProvider),
  ),
);
```

**配套改造 `app.dart`**：将手动 `new` 改为 `ref.read`，保持单例。

`lib/app.dart` 第 62-68 行改动：

改动前：
```dart
    final platform = ref.read(platformServiceProvider);
    final fileStore = ref.read(fileStoreProvider);

    _systemIntegration = SystemIntegrationService(
      fileStore: fileStore,
      platformService: platform,
    );
```

改动后：
```dart
    // 改用全局 Provider，确保设置页与 app 用同一实例
    _systemIntegration = ref.read(systemIntegrationServiceProvider);
```

> 注意：`app.dart` import 区需新增 `import 'core/state/providers.dart';`（若未引入）。`platformServiceProvider`/`fileStoreProvider` 的 import 若因此变为未使用，保留即可（其他方法仍用）。

---

### 步骤 3：app.dart 改造 _initAutoStart()

**文件**：`lib/app.dart`

**定位**：第 246-255 行。

**改动前**：
```dart
  // ──────────────────────────── 开机自启 ────────────────────────────

  void _initAutoStart() {
    final platform = ref.read(platformServiceProvider);
    if (!platform.supportsAutoStart) return;

    // 默认启用自启（与 schtasks ONLOGON 行为一致）
    _systemIntegration!.setAutoStart(true);
    _logger.info('开机自启已配置');
  }
```

**改动后**：
```dart
  // ──────────────────────────── 开机自启 ────────────────────────────

  Future<void> _initAutoStart() async {
    final platform = ref.read(platformServiceProvider);
    if (!platform.supportsAutoStart) {
      _logger.debug('当前平台不支持开机自启，跳过');
      return;
    }

    // 读取用户配置，按需注册/注销自启（不再硬编码 true）
    final config = ref.read(configStoreProvider).valueOrNull;
    final enabled = config?.launchAtStartup ?? false;

    await _systemIntegration!.setAutoStart(enabled);
    _logger.info('开机自启按配置设置：${enabled ? "已启用" : "已禁用"}');
  }
```

**配套**：第 90 行调用处，`_initAutoStart();` 改为 `await _initAutoStart();`（`_initializeSystemIntegration` 已是 async）。

> **兼容旧版本（可选）**：若希望迁移期保留「旧版默认自启」体验，在 `migration.dart` 迁移完成后判断：
> ```dart
> if (config.firstRun && await systemIntegration.isAutoStartEnabled()) {
>   config.launchAtStartup = true;   // 旧版已注册，迁移为开启
> }
> ```

---

### 步骤 4：设置页新增「系统」区块与开关

**文件**：`lib/features/settings/presentation/settings_page.dart`

#### 4.1 新增状态字段与依赖

第 43-46 行状态区追加：
```dart
  bool _etEnableIpv6 = false;
  bool _etEnableWebDl = false;
  bool _etEnableUserConf = false;
  bool _autoStartEnabled = false;   // ← 新增：开机自启开关状态
  bool _autoStartSupported = true;  // ← 新增：当前平台是否支持
  bool _isLoading = false;
```

#### 4.2 加载配置时同步真实状态

第 67-85 行 `_loadConfig()` 内追加（在现有 setState 之后）：

```dart
  void _loadConfig() async {
    final config = ref.read(configStoreProvider).valueOrNull;
    if (config == null || !mounted) return;

    _esurfingUrlController.text = config.esurfingUrl ?? '';
    _wlanAcIpController.text = config.wlanAcIp ?? '';
    _wlanUserIpController.text = config.wlanUserIp ?? '';

    _etSecretKeyController.text =
        config.etSecretKey ?? EsurfingApi.etDefaultSecret;
    _etSpeedLimitController.text = config.etSpeedLimit ?? '';

    // ── 新增：开机自启状态同步 ──
    final platform = ref.read(platformServiceProvider);
    final supported = platform.supportsAutoStart;
    // 系统真实状态可能被外部（任务管理器/系统设置）改变，以 isAutoStartEnabled() 为准
    bool realEnabled = false;
    if (supported) {
      final sys = ref.read(systemIntegrationServiceProvider);
      realEnabled = await sys.isAutoStartEnabled();
      // 若系统状态与配置不一致，以系统为准回写配置
      if (realEnabled != config.launchAtStartup) {
        config.launchAtStartup = realEnabled;
      }
    }

    if (!mounted) return;
    setState(() {
      _etEnableIpv6 = config.etEnableIpv6;
      _etEnableWebDl = config.etEnableWebDl;
      _etEnableUserConf = config.etEnableUserConf;
      _etUserConfPathController.text = config.etUserConfPath ?? '';
      _autoStartEnabled = realEnabled;
      _autoStartSupported = supported;
    });
  }
```

> **关键**：`_loadConfig` 改为 `async`。原本同步调用（`_loadConfig()` in `addPostFrameCallback`）仍可用，无需改调用点。注意 Flutter 技能规则：异步间隙后用 `mounted` 守卫，已包含。

#### 4.3 新增「系统」区块 UI

在第 209 行（隧道配置区块结束 `]` 之后、`const SizedBox(height: 24)` 之前）插入新区块。即「隧道配置」与「配置管理」之间：

```dart
          // ── 系统设置（新增） ──
          const SizedBox(height: 24),
          const Divider(),
          _buildSectionHeader(l10n.systemSettings, Icons.desktop_windows),
          const SizedBox(height: 8),
          // 开机自启开关
          SwitchListTile(
            title: Text(l10n.launchAtStartupTitle),
            subtitle: Text(_autoStartSupported
                ? l10n.launchAtStartupSubtitle
                : l10n.launchAtStartupNotSupported),
            value: _autoStartEnabled,
            onChanged: _autoStartSupported ? _onAutoStartChanged : null,
            dense: true,
            contentPadding: EdgeInsets.zero,
            // 禁用态视觉
            activeSwitchColor: Theme.of(context).colorScheme.primary,
          ),
```

#### 4.4 开关切换处理方法

在 `_saveConfig` 之前（或类内任意方法区）新增：

```dart
  /// 开机自启开关切换
  Future<void> _onAutoStartChanged(bool value) async {
    final l10n = AppLocalizations.of(context);
    final config = ref.read(configStoreProvider).valueOrNull;
    final sys = ref.read(systemIntegrationServiceProvider);
    final prev = _autoStartEnabled;

    // 乐观更新 UI（即时反馈）
    setState(() => _autoStartEnabled = value);

    try {
      await sys.setAutoStart(value);
      // 持久化用户选择
      if (config != null) config.launchAtStartup = value;

      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text(
            value ? l10n.launchAtStartupEnabled : l10n.launchAtStartupDisabled,
          )),
        );
      }
    } catch (e) {
      // 失败回滚 UI
      if (mounted) {
        setState(() => _autoStartEnabled = prev);
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text('${l10n.launchAtStartupFailed}: $e')),
        );
      }
    }
  }
```

#### 4.5 import 补充

文件顶部 import 区确保含：
```dart
import '../../../core/state/providers.dart';   // systemIntegrationServiceProvider（若 core.dart 已 export 则免）
```

> 若 `core/core.dart` 已 barrel export `providers.dart`，则通过 `import '../../../core/core.dart';` 即可（当前已有该 import，确认 `core.dart` 是否 export providers）。

---

### 步骤 5：l10n 文案

#### 5.1 `lib/l10n/app_zh.arb` 追加键值

在文件末尾 `}` 之前追加（注意上一行末尾加逗号）：

```json
  "systemSettings": "系统设置",
  "@systemSettings": { "description": "系统设置区块标题" },
  "launchAtStartupTitle": "开机自启",
  "launchAtStartupSubtitle": "登录 Windows 时自动启动绳网认证",
  "launchAtStartupNotSupported": "当前平台不支持开机自启（仅桌面端）",
  "launchAtStartupEnabled": "开机自启已启用",
  "launchAtStartupDisabled": "开机自启已关闭",
  "launchAtStartupFailed": "设置开机自启失败"
```

#### 5.2 `lib/l10n/app_en.arb` 追加

```json
  "systemSettings": "System",
  "launchAtStartupTitle": "Launch at startup",
  "launchAtStartupSubtitle": "Start InterKnot Auth automatically when you sign in",
  "launchAtStartupNotSupported": "Not supported on this platform (desktop only)",
  "launchAtStartupEnabled": "Launch at startup enabled",
  "launchAtStartupDisabled": "Launch at startup disabled",
  "launchAtStartupFailed": "Failed to set launch at startup"
```

#### 5.3 重新生成本地化代码

项目若用代码生成（`l10n.yaml` + `flutter gen-l10n`）：
```bash
cd interknot_auth_flutter
flutter gen-l10n
```

若 `app_localizations.dart` / `app_localizations_zh.dart` / `app_localizations_en.dart` 为**手写**（从文件名看可能是），则需手动在三个文件分别补 getter：

`app_localizations.dart`（抽象类）：
```dart
  String get systemSettings;
  String get launchAtStartupTitle;
  String get launchAtStartupSubtitle;
  String get launchAtStartupNotSupported;
  String get launchAtStartupEnabled;
  String get launchAtStartupDisabled;
  String get launchAtStartupFailed;
```

`app_localizations_zh.dart`：
```dart
  @override
  String get systemSettings => '系统设置';
  @override
  String get launchAtStartupTitle => '开机自启';
  @override
  String get launchAtStartupSubtitle => '登录 Windows 时自动启动绳网认证';
  @override
  String get launchAtStartupNotSupported => '当前平台不支持开机自启（仅桌面端）';
  @override
  String get launchAtStartupEnabled => '开机自启已启用';
  @override
  String get launchAtStartupDisabled => '开机自启已关闭';
  @override
  String get launchAtStartupFailed => '设置开机自启失败';
```

`app_localizations_en.dart`：对应英文串。

---

### 步骤 6：平台适配与边界处理

| 场景 | 处理 |
|------|------|
| 移动端（Android/iOS） | `platform.supportsAutoStart == false` → 开关 `onChanged: null`（禁用），副标题显示「不支持」 |
| 系统状态被外部改变 | 进入设置页调 `isAutoStartEnabled()` 同步真实状态并回写配置（步骤 4.2） |
| 设置失败（权限/异常） | `setAutoStart` 抛错 → UI 回滚 + SnackBar 提示（步骤 4.4） |
| 首次运行 | `launchAtStartup` 默认 `false`，不自启；用户主动开启后才注册 |
| 桌面端需管理员权限 | `launch_at_startup` 在 Windows 用当前用户级注册表（HKCU Run），**无需管理员**；若改用 schtasks ONLOGON/HIGHEST 则需提权（当前实现走 launch_at_startup，无需改） |

---

## 4. 关键代码合规要点（遵循 Flutter 技能规则）

1. **mounted 守卫**：`_loadConfig`、`_onAutoStartChanged` 中所有 `setState`/`ScaffoldMessenger` 前均检查 `mounted`（异步间隙后 context 可能失效）。
2. **dispose 清理**：本改动无新增 Timer/Controller/Subscription，`dispose` 无需修改。
3. **const 优化**：`SwitchListTile` 的 `title`/`subtitle` 文案来自 l10n 无法 const，但区块标题 `_buildSectionHeader(l10n.systemSettings, Icons.desktop_windows)` 中 `Icon` 可保持默认；无静态 widget 需 const 化。
4. **乐观更新 + 回滚**：切换开关先改 UI 再 await，失败回滚 `prev`，避免界面卡顿。
5. **不持 BuildContext 跨异步**：`_onAutoStartChanged` 内 await 后通过 `mounted` 守卫再用 `context`，符合规则。

---

## 5. 测试与验收清单

### 5.1 单元测试（可选，`test/core/storage/config_store_test.dart`）
- [ ] `launchAtStartup` 默认 `false`
- [ ] setter 写入后 getter 读取一致
- [ ] `clearAll()` 后恢复默认 `false`

### 5.2 手动验收（Windows 桌面）
- [ ] 首次启动，设置页「开机自启」开关为关闭状态。
- [ ] 打开开关 → SnackBar「已启用」 → 重启系统后程序自启动。
- [ ] 关闭开关 → SnackBar「已关闭」 → 重启系统后不再自启。
- [ ] 在 Windows 任务管理器「启动」页手动禁用 → 重开程序进入设置页，开关自动变为关闭（状态同步）。
- [ ] 移动端构建（Android）打开设置页，开关禁用，副标题显示「不支持」。
- [ ] 权限/异常场景：模拟 `setAutoStart` 抛错，UI 回滚，SnackBar 提示失败。
- [ ] `flutter analyze` 0 error。

### 5.3 回归
- [ ] 原有隧道配置、登录参数、配置管理三段功能不受影响。
- [ ] app.dart 启动流程无报错，日志含「开机自启按配置设置：已启用/已禁用」。

---

## 6. 风险与回滚

| 风险 | 缓解 |
|------|------|
| 旧版本用户升级后失去自启 | 迁移工具检测旧 schtasks 任务，存在则 `launchAtStartup=true`（步骤 3 备注） |
| `systemIntegrationServiceProvider` 与 app.dart 实例化冲突 | 步骤 2 将 app.dart 改为 `ref.read(provider)`，确保全局单例 |
| l10n 生成失败 | 改用手写 getter（步骤 5.3 给出两手写文件补法） |
| 用户误关自启后无法开机启动 | 设置页副标题清晰说明；开关状态可随时重新打开 |

**回滚**：本次改动均为增量，回滚只需 git revert 对应 commit；`app.dart` 步骤 3 可单独回退为硬编码 `true`（但不推荐）。

---

## 7. 改动文件清单速查

```
lib/core/storage/config_store.dart                                    [步骤1]
lib/core/state/providers.dart                                         [步骤2]
lib/app.dart                                                         [步骤2配套 + 步骤3]
lib/features/settings/presentation/settings_page.dart                [步骤4]
lib/l10n/app_zh.arb                                                  [步骤5]
lib/l10n/app_en.arb                                                  [步骤5]
lib/l10n/app_localizations.dart / app_localizations_zh.dart / _en    [步骤5.3，若手写]
```

---

**文档版本**：1.0 · 生成于 2026-08-11
**前置依赖**：项目已完成 M0-M4 迁移（`SystemIntegrationService` / `launch_at_startup` / `ConfigStore` / `PlatformService` 均已就绪）
