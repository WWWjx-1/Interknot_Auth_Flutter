# 最小化至托盘开关与托盘退出菜单改造方案

> **目标项目**：`interknot_auth_flutter`（v2.0）
> **改造范围**：
>   1. 设置页新增「关闭时最小化至托盘」手动开关，支持用户选择「关闭即退出」或「关闭即最小化」
>   2. 优化托盘右键菜单：确保「退出」按钮可用、文案国际化、退出行为合理
> **难度**：中等（5 处文件改动）
> **预计工期**：0.5 人日
> **前置状态**：上一轮「开机自启手动开关」改造已落地（`systemIntegrationServiceProvider` 已注册、`launchAtStartup` 配置项已存在、settings_page「系统」区块已存在）

---

## 1. 背景与现状澄清

### 1.1 现状梳理（重要：先读懂再改）

经核查当前代码，两个需求的真实情况不同：

#### 需求一：托盘右键「退出」按钮
**代码上已存在**，并非缺失：

- `lib/core/platform/system_integration_service.dart:94-106` 的 `initSystemTray()` 已设置右键菜单：
  ```dart
  final menu = Menu(items: [
    MenuItem(key: 'show', label: '显示主窗口'),
    MenuItem.separator(),
    MenuItem(key: 'exit',  label: '退出'),
  ]);
  await trayManager.setContextMenu(menu);
  ```
- `lib/app.dart:138-146` 的 `onTrayMenuItemClick` 已处理 `case 'exit'` → `_onTrayExit()` → `_showExitConfirmDialog()` → 确认后 `_exitApp()`。

**但存在三个可优化点**：
1. 菜单文案硬编码中文（`"显示主窗口"`/`"退出"`），未走 l10n，无法跟随系统语言。
2. 退出会弹确认对话框（防误触），部分用户期望「直接退出」。
3. `initSystemTray` 在 service 层无法直接拿 `BuildContext`/`AppLocalizations`，文案需由调用方传入。

#### 需求二：最小化至托盘手动开关
**确实缺失**，是本次改造重点：

- `lib/app.dart:81` 硬编码 `await _systemIntegration!.setPreventClose(true);`（关闭按钮永远被拦截）。
- `lib/app.dart:150-154` `onWindowClose()` 固定调 `minimizeToTray()`，用户无法选「关闭即退出」。
- `ConfigStore` 无 `minimizeToTray` 配置项。

### 1.2 改造目标

- 新增 `minimizeToTray` 配置项，默认 `true`（保持原行为，向后兼容）。
- `onWindowClose` 按配置分流：`true`→最小化至托盘，`false`→直接退出（走 `_exitApp` 清理资源）。
- 设置页「系统」区块（已有开机自启开关）追加「关闭时最小化至托盘」开关。
- 托盘菜单文案国际化（由 app.dart 传入 l10n 字符串）。
- 提供「直接退出」托盘菜单项（可选，防误触与便捷兼顾）。

---

## 2. 改造方案总览

### 2.1 改动清单（5 处）

| # | 文件 | 改动 | 类型 |
|---|------|------|------|
| 1 | `lib/core/storage/config_store.dart` | 新增 `minimizeToTray` 配置项 | 增量 |
| 2 | `lib/app.dart` | `setPreventClose` 固定 true + `onWindowClose` 按配置分流 + 传 l10n 给托盘 | 修改 |
| 3 | `lib/core/platform/system_integration_service.dart` | `initSystemTray` 接收菜单文案参数；新增「直接退出」项 | 修改 |
| 4 | `lib/features/settings/presentation/settings_page.dart` | 系统区块追加「最小化至托盘」开关 | 增量 |
| 5 | `lib/l10n/app_zh.arb` + `app_en.arb` + `app_localizations*.dart` | 新增文案 | 增量 |

### 2.2 行为矩阵

| 用户操作 | `minimizeToTray=true` | `minimizeToTray=false` |
|----------|----------------------|------------------------|
| 点窗口关闭按钮(×) | 隐藏到托盘 | 直接退出（清理资源后 `exit(0)`） |
| 托盘右键 → 显示主窗口 | 显示并聚焦 | 显示并聚焦 |
| 托盘右键 → 退出 | 弹确认框 → 退出 | 弹确认框 → 退出 |
| 托盘右键 → 直接退出（新增，可选） | 立即退出 | 立即退出 |

---

## 3. 逐步实现

### 步骤 1：ConfigStore 新增配置项

**文件**：`lib/core/storage/config_store.dart`

**定位**：第 60 行 `launchAtStartup` setter 之后（开关配置区块内）。

**改动前**（第 55-60 行）：
```dart
  bool get launchAtStartup => _sp.getBool('launch_at_startup') ?? false;
  set launchAtStartup(bool v) => _sp.setBool('launch_at_startup', v);
```

**改动后**（其后追加）：
```dart
  bool get launchAtStartup => _sp.getBool('launch_at_startup') ?? false;
  set launchAtStartup(bool v) => _sp.setBool('launch_at_startup', v);

  /// 关闭窗口时最小化至托盘（true）或直接退出（false）
  ///
  /// 默认 true，保持原「关闭即最小化」行为。
  /// 切换后对当前会话即时生效（onWindowClose 读取此配置分流）。
  bool get minimizeToTray => _sp.getBool('minimize_to_tray') ?? true;
  set minimizeToTray(bool v) => _sp.setBool('minimize_to_tray', v);
```

> **默认 true** 的理由：原程序行为即「关闭最小化到托盘」，保持向后兼容；用户可在设置页主动改为「关闭即退出」。

---

### 步骤 2：app.dart 改造关闭行为与托盘初始化

**文件**：`lib/app.dart`

#### 2.1 托盘初始化传入 l10n 文案

**定位**：第 76-83 行（托盘初始化区块）。

**改动前**：
```dart
    // 2. 初始化系统托盘
    if (platform.supportsSystemTray) {
      await _systemIntegration!.initSystemTray(listener: this);

      // 设置关闭时最小化到托盘
      await _systemIntegration!.setPreventClose(true);
      _systemIntegration!.addWindowListener(this);
    }
```

**改动后**：
```dart
    // 2. 初始化系统托盘
    if (platform.supportsSystemTray) {
      final l10n = AppLocalizations.of(context);
      await _systemIntegration!.initSystemTray(
        listener: this,
        showLabel: l10n.trayShowWindow,
        exitLabel: l10n.trayExit,
        quickExitLabel: l10n.trayQuickExit,  // 新增「直接退出」项
      );

      // 始终拦截关闭按钮，由 onWindowClose 按配置分流
      // （setPreventClose(true) 后点 × 不会真关，触发 onWindowClose）
      await _systemIntegration!.setPreventClose(true);
      _systemIntegration!.addWindowListener(this);
    }
```

> 说明：`setPreventClose(true)` 固定不变，无论 `minimizeToTray` 取值，关闭按钮都被拦截，由 `onWindowClose` 内部按配置决定行为。这样退出路径统一走 `_exitApp()`，保证资源（托盘/监听器/文件锁）被清理。

#### 2.2 onWindowClose 按配置分流

**定位**：第 150-154 行。

**改动前**：
```dart
  @override
  void onWindowClose() {
    // 最小化到托盘而非退出
    _systemIntegration?.minimizeToTray();
  }
```

**改动后**：
```dart
  @override
  void onWindowClose() {
    // 按配置分流：true=最小化到托盘，false=直接退出
    final config = ref.read(configStoreProvider).valueOrNull;
    final toTray = config?.minimizeToTray ?? true;

    if (toTray) {
      _systemIntegration?.minimizeToTray();
    } else {
      _exitApp();   // 走统一退出路径，清理托盘/锁/监听器后 exit(0)
    }
  }
```

> **Flutter 合规**：`onWindowClose` 是同步回调，用 `ref.read`（非 `ref.watch`）同步读已缓存的 `ConfigStore`，合法。无需 `mounted` 守卫（此回调不在 element 树构建期，且 `_exitApp`/`minimizeToTray` 均不依赖 `context`）。

#### 2.3 托盘菜单点击处理新增「直接退出」

**定位**：第 138-146 行 `onTrayMenuItemClick`。

**改动前**：
```dart
  @override
  void onTrayMenuItemClick(MenuItem menuItem) {
    switch (menuItem.key) {
      case 'show':
        _systemIntegration?.restoreFromTray();
      case 'exit':
        _onTrayExit();
    }
  }
```

**改动后**：
```dart
  @override
  void onTrayMenuItemClick(MenuItem menuItem) {
    switch (menuItem.key) {
      case 'show':
        _systemIntegration?.restoreFromTray();
      case 'exit':
        _onTrayExit();          // 弹确认框后退出（防误触）
      case 'quick_exit':         // 新增：直接退出，不弹框
        _exitApp();
    }
  }
```

> 设计：保留 `exit`（确认后退出）+ 新增 `quick_exit`（直接退出），兼顾防误触与便捷。若不想加「直接退出」，可省略 `quick_exit` 项，仅保留 `exit`。

---

### 步骤 3：SystemIntegrationService 托盘菜单优化

**文件**：`lib/core/platform/system_integration_service.dart`

**定位**：第 77-117 行 `initSystemTray`。

**改动前**（第 80-108 行核心）：
```dart
  Future<void> initSystemTray({required TrayListener listener}) async {
    if (!_platformService.supportsSystemTray) {
      _logger.debug('当前平台不支持系统托盘');
      return;
    }

    try {
      await trayManager.setIcon('assets/icons/yish.ico');
      await trayManager.setToolTip('绳网认证 2.0');

      final menu = Menu(
        items: [
          MenuItem(key: 'show', label: '显示主窗口'),
          MenuItem.separator(),
          MenuItem(key: 'exit', label: '退出'),
        ],
      );
      await trayManager.setContextMenu(menu);
      trayManager.addListener(listener);
      _logger.info('系统托盘初始化成功');
    } catch (e, s) {
      _logger.error('系统托盘初始化失败', e, s);
    }
  }
```

**改动后**：
```dart
  /// 初始化系统托盘
  ///
  /// [listener] 托盘事件监听器
  /// [showLabel] / [exitLabel] / [quickExitLabel] 菜单文案（由调用方传入 l10n）
  Future<void> initSystemTray({
    required TrayListener listener,
    required String showLabel,
    required String exitLabel,
    String? quickExitLabel,   // 为 null 时不显示「直接退出」项
  }) async {
    if (!_platformService.supportsSystemTray) {
      _logger.debug('当前平台不支持系统托盘');
      return;
    }

    try {
      await trayManager.setIcon('assets/icons/yish.ico');
      await trayManager.setToolTip('绳网认证 2.0');

      final items = <MenuItem>[
        MenuItem(key: 'show', label: showLabel),
        MenuItem.separator(),
        MenuItem(key: 'exit', label: exitLabel),
      ];
      // 可选：追加「直接退出」项
      if (quickExitLabel != null) {
        items.add(MenuItem.separator());
        items.add(MenuItem(key: 'quick_exit', label: quickExitLabel));
      }

      final menu = Menu(items: items);
      await trayManager.setContextMenu(menu);
      trayManager.addListener(listener);
      _logger.info('系统托盘初始化成功');
    } catch (e, s) {
      _logger.error('系统托盘初始化失败', e, s);
    }
  }
```

> **确保右键菜单显示的要点**：
> - 调用顺序：`setIcon` → `setToolTip` → `setContextMenu` → `addListener`（当前顺序正确）。
> - 若实测右键无菜单：检查 `assets/icons/yish.ico` 是否已声明在 `pubspec.yaml` 的 `assets:` 下（未声明则 `setIcon` 失败，整个 try 块可能中断）。
> - Windows 首次运行可能需管理员权限或被杀软拦截托盘，建议在设置页提供「重启托盘」入口（可选增强）。

---

### 步骤 4：设置页系统区块追加「最小化至托盘」开关

**文件**：`lib/features/settings/presentation/settings_page.dart`

#### 4.1 新增状态字段

**定位**：第 46-48 行状态区。

**改动前**：
```dart
  bool _autoStartEnabled = false;
  bool _autoStartSupported = true;
  bool _isLoading = false;
```

**改动后**：
```dart
  bool _autoStartEnabled = false;
  bool _autoStartSupported = true;
  bool _minimizeToTray = true;       // 新增：关闭时最小化至托盘
  bool _traySupported = true;        // 新增：当前平台是否支持托盘
  bool _isLoading = false;
```

#### 4.2 加载配置时读取

**定位**：第 96-103 行 `setState` 内。

**改动前**：
```dart
    setState(() {
      _etEnableIpv6 = config.etEnableIpv6;
      _etEnableWebDl = config.etEnableWebDl;
      _etEnableUserConf = config.etEnableUserConf;
      _etUserConfPathController.text = config.etUserConfPath ?? '';
      _autoStartEnabled = realEnabled;
      _autoStartSupported = supported;
    });
```

**改动后**：
```dart
    setState(() {
      _etEnableIpv6 = config.etEnableIpv6;
      _etEnableWebDl = config.etEnableWebDl;
      _etEnableUserConf = config.etEnableUserConf;
      _etUserConfPathController.text = config.etUserConfPath ?? '';
      _autoStartEnabled = realEnabled;
      _autoStartSupported = supported;
      _minimizeToTray = config.minimizeToTray;
      _traySupported = platform.supportsSystemTray;   // 复用已 read 的 platform
    });
```

> 注意：第 82 行已 `final platform = ref.read(platformServiceProvider);`，此处直接复用。

#### 4.3 UI 追加开关

**定位**：第 245 行（开机自启 `SwitchListTile` 结束的 `),` 之后）。

**改动前**（第 234-245 行）：
```dart
          SwitchListTile(
            title: Text(l10n.launchAtStartupTitle),
            subtitle: Text(_autoStartSupported
                ? l10n.launchAtStartupSubtitle
                : l10n.launchAtStartupNotSupported),
            value: _autoStartEnabled,
            onChanged: _autoStartSupported ? _onAutoStartChanged : null,
            dense: true,
            contentPadding: EdgeInsets.zero,
            activeTrackColor: Theme.of(context).colorScheme.primary.withValues(alpha: 0.5),
            activeThumbColor: Theme.of(context).colorScheme.primary,
          ),
```

**改动后**（在其后追加一个 `SwitchListTile`）：
```dart
          SwitchListTile(
            title: Text(l10n.launchAtStartupTitle),
            subtitle: Text(_autoStartSupported
                ? l10n.launchAtStartupSubtitle
                : l10n.launchAtStartupNotSupported),
            value: _autoStartEnabled,
            onChanged: _autoStartSupported ? _onAutoStartChanged : null,
            dense: true,
            contentPadding: EdgeInsets.zero,
            activeTrackColor: Theme.of(context).colorScheme.primary.withValues(alpha: 0.5),
            activeThumbColor: Theme.of(context).colorScheme.primary,
          ),
          // 新增：关闭时最小化至托盘开关
          SwitchListTile(
            title: Text(l10n.minimizeToTrayTitle),
            subtitle: Text(_traySupported
                ? l10n.minimizeToTraySubtitle
                : l10n.minimizeToTrayNotSupported),
            value: _minimizeToTray,
            onChanged: _traySupported ? _onMinimizeToTrayChanged : null,
            dense: true,
            contentPadding: EdgeInsets.zero,
          ),
```

#### 4.4 切换处理方法

**定位**：在 `_onAutoStartChanged`（第 316 行）之后新增方法。

```dart
  /// 最小化至托盘开关切换
  ///
  /// 仅持久化配置，当前会话即时生效（onWindowClose 读取此配置）。
  /// setPreventClose 始终为 true，无需在此调用。
  Future<void> _onMinimizeToTrayChanged(bool value) async {
    final l10n = AppLocalizations.of(context);
    final config = ref.read(configStoreProvider).valueOrNull;
    final prev = _minimizeToTray;

    setState(() => _minimizeToTray = value);

    try {
      if (config != null) config.minimizeToTray = value;
      if (mounted) {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text(
            value ? l10n.minimizeToTrayEnabled : l10n.minimizeToTrayDisabled,
          )),
        );
      }
    } catch (e) {
      if (mounted) {
        setState(() => _minimizeToTray = prev);
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text('${l10n.minimizeToTrayFailed}: $e')),
        );
      }
    }
  }
```

> **设计说明**：
> - `setPreventClose(true)` 在启动时固定，切换开关**无需**调 `setPreventClose`，因为关闭按钮始终被拦截，行为由 `onWindowClose` 读配置决定。
> - 因此切换开关只需持久化 `config.minimizeToTray`，`onWindowClose` 下次触发时即时读取生效。
> - 乐观更新 + 失败回滚，符合 Flutter 技能规则。

#### 4.5 _clearConfig 重置追加

**定位**：第 450-454 行 `setState`（清除配置后重置表单）。

**改动前**：
```dart
      setState(() {
        _etEnableIpv6 = false;
        _etEnableWebDl = false;
        _etEnableUserConf = false;
      });
```

**改动后**：
```dart
      setState(() {
        _etEnableIpv6 = false;
        _etEnableWebDl = false;
        _etEnableUserConf = false;
        _minimizeToTray = true;   // 清除后恢复默认（true）
      });
```

---

### 步骤 5：l10n 文案

#### 5.1 `lib/l10n/app_zh.arb` 追加

```json
  "minimizeToTrayTitle": "关闭时最小化至托盘",
  "minimizeToTraySubtitle": "关闭窗口时隐藏到系统托盘；关闭后改为直接退出",
  "minimizeToTrayNotSupported": "当前平台不支持系统托盘",
  "minimizeToTrayEnabled": "已设置为：关闭时最小化至托盘",
  "minimizeToTrayDisabled": "已设置为：关闭时直接退出",
  "minimizeToTrayFailed": "设置失败",
  "trayShowWindow": "显示主窗口",
  "trayExit": "退出",
  "trayQuickExit": "直接退出"
```

#### 5.2 `lib/l10n/app_en.arb` 追加

```json
  "minimizeToTrayTitle": "Minimize to tray on close",
  "minimizeToTraySubtitle": "Hide to system tray when closing; turn off to quit directly",
  "minimizeToTrayNotSupported": "System tray not supported on this platform",
  "minimizeToTrayEnabled": "Minimize to tray on close: enabled",
  "minimizeToTrayDisabled": "Quit directly on close: enabled",
  "minimizeToTrayFailed": "Failed to update",
  "trayShowWindow": "Show main window",
  "trayExit": "Exit",
  "trayQuickExit": "Quit now"
```

#### 5.3 重新生成本地化代码

```bash
cd interknot_auth_flutter
flutter gen-l10n
```

若 `app_localizations*.dart` 为手写，则在 `app_localizations.dart`（抽象类）补 getter：

```dart
  String get minimizeToTrayTitle;
  String get minimizeToTraySubtitle;
  String get minimizeToTrayNotSupported;
  String get minimizeToTrayEnabled;
  String get minimizeToTrayDisabled;
  String get minimizeToTrayFailed;
  String get trayShowWindow;
  String get trayExit;
  String get trayQuickExit;
```

`app_localizations_zh.dart`：
```dart
  @override String get minimizeToTrayTitle => '关闭时最小化至托盘';
  @override String get minimizeToTraySubtitle => '关闭窗口时隐藏到系统托盘；关闭后改为直接退出';
  @override String get minimizeToTrayNotSupported => '当前平台不支持系统托盘';
  @override String get minimizeToTrayEnabled => '已设置为：关闭时最小化至托盘';
  @override String get minimizeToTrayDisabled => '已设置为：关闭时直接退出';
  @override String get minimizeToTrayFailed => '设置失败';
  @override String get trayShowWindow => '显示主窗口';
  @override String get trayExit => '退出';
  @override String get trayQuickExit => '直接退出';
```

`app_localizations_en.dart`：对应英文串。

---

## 4. 关键合规要点（遵循 Flutter 技能规则）

1. **mounted 守卫**：`_onMinimizeToTrayChanged` 中所有 `setState`/`ScaffoldMessenger` 前检查 `mounted`（异步间隙后 context 可能失效）。
2. **同步读 Provider**：`onWindowClose` 是同步回调，用 `ref.read(configStoreProvider).valueOrNull`（非 `watch`）同步读已缓存值，合法且高效。
3. **乐观更新 + 回滚**：切换开关先改 UI 再持久化，失败回滚 `prev`。
4. **dispose 清理**：本次无新增 Timer/Controller/Subscription，`dispose` 无需修改。退出路径统一走 `_exitApp()`，已含 `disposeTray`/`removeWindowListener`/`releaseLock`。
5. **const 优化**：新增 `SwitchListTile` 的文案来自 l10n 无法 const，但不影响性能（设置页非高频 rebuild）。
6. **不持 BuildContext 跨异步**：`_onMinimizeToTrayChanged` await 后通过 `mounted` 守卫再用 `context`。

---

## 5. 测试与验收清单

### 5.1 功能验收（Windows 桌面）
- [ ] 设置页「系统」区块出现两个开关：开机自启 + 关闭时最小化至托盘。
- [ ] 「最小化至托盘」默认开启（首次安装/清除配置后）。
- [ ] 开启状态下点窗口 × → 窗口隐藏，托盘图标存在，程序仍在运行。
- [ ] 关闭开关后点窗口 × → 程序直接退出，托盘图标消失，文件锁释放（可立即重开）。
- [ ] 托盘右键 → 弹出菜单含「显示主窗口」「退出」（及可选「直接退出」）。
- [ ] 点「显示主窗口」→ 窗口恢复并聚焦。
- [ ] 点「退出」→ 弹确认框 → 确认后退出。
- [ ] 点「直接退出」（若有）→ 立即退出，无确认框。
- [ ] 切换开关 → SnackBar 提示「已设置为...」。
- [ ] 重启程序，开关状态保持上次选择。
- [ ] 中英文切换，托盘菜单与开关文案随之变化。

### 5.2 平台适配
- [ ] 移动端构建：托盘开关禁用，副标题「不支持」；不报错。
- [ ] `flutter analyze` 0 error。

### 5.3 回归
- [ ] 开机自启开关功能不受影响。
- [ ] 登录/隧道/配置管理功能正常。

---

## 6. 风险与回滚

| 风险 | 缓解 |
|------|------|
| `onWindowClose` 读 config 时 ConfigStore 未就绪 | `valueOrNull ?? true` 兜底，默认最小化（安全降级） |
| 托盘菜单右键不显示 | 检查 `assets/icons/yish.ico` 是否在 pubspec assets 声明；检查 `setIcon` 是否失败（日志「系统托盘初始化失败」） |
| 用户误选「关闭即退出」丢失托盘常驻 | 默认 true；副标题明确说明；可随时改回 |
| 直接退出未清理资源 | `quick_exit` 走 `_exitApp()`，与确认退出同路径，已含资源清理 |

**回滚**：改动均为增量，git revert 即可。`app.dart` 步骤 2 可单独回退为原 `onWindowClose` 固定 `minimizeToTray()`。

---

## 7. 改动文件清单速查

```
lib/core/storage/config_store.dart                                 [步骤1]
lib/app.dart                                                       [步骤2.1/2.2/2.3]
lib/core/platform/system_integration_service.dart                  [步骤3]
lib/features/settings/presentation/settings_page.dart              [步骤4]
lib/l10n/app_zh.arb + app_en.arb + app_localizations*.dart         [步骤5]
```

---

## 8. 与既有改造的关系

- 本方案**复用**上一轮「开机自启」改造建立的：
  - `systemIntegrationServiceProvider`（步骤 4 直接 `ref.read` 调用 `setPreventClose`/`minimizeToTray` 等）
  - settings_page「系统」区块结构（在开机自启开关后追加）
  - `_onAutoStartChanged` 的「乐观更新+回滚」模式（步骤 4.4 同构）
- 不影响 `launch_at_startup` 相关逻辑，两者独立。

---

**文档版本**：1.0 · 生成于 2026-08-11
**现状基准**：上一轮开机自启改造已落地（`launchAtStartup` 配置项、`systemIntegrationServiceProvider`、settings 系统区块均已就绪）
